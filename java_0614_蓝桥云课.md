# Java 基础语法与面向对象笔记 (0614)

## 一、final 关键字

`final` 关键字可以修饰类、方法、属性和变量。

1. **修饰类**：则该类不允许被继承，为最终类。
2. **修饰方法**：则该方法不允许被覆盖（重写）。
3. **修饰属性**：则该类的属性不会进行隐式的初始化（类的初始化属性必须有值）或在构造方法中赋值（只能选其一）。
4. **修饰变量**：则该变量的值只能赋一次值，即成为常量。

**示例：静态常量**
```java
public final static String SHI_YAN_LOU = "shiyanlou";
```

---

## 二、super 关键字

`super` 关键字在子类内部使用，代表父类对象。

1. 访问父类的属性：`super.属性名`。
2. 访问父类的方法：`super.bark()`。
3. 子类构造方法需要调用父类的构造方法时，在子类的构造方法体里最前面的位置：`super()`。

### 变量查找顺序

| 写法 | 查找顺序 |
|---|---|
| 不加前缀 | 方法局部变量 → 子类成员变量 → 父类成员变量 |
| `this.` | 子类成员变量 → 父类成员变量 |
| `super.` | 直接去父类成员变量 |

### 实际运用场景

最常见的场景有两个：

**1. `super()` 调构造方法 → 几乎每个继承都会用到**
只要写了继承，子类的构造方法里基本都要调 `super()` 来初始化父类。这是最高频的用法。

**2. `super.方法()` → 方法重写的时候常用**
比如子类重写了父类的方法，但又想在父类的基础上加点东西，这种"先调爸爸的，再加自己的"写法在实际开发中非常常见。

**3. `super.属性` → 相对少一些**
因为正规开发中，父类的属性一般都设成 `private`，子类根本访问不到，得通过父类的 getter 方法来拿。直接写 `super.属性` 的场景不算多，但**考试喜欢考**。

---

## 三、上转型 (Upcasting)

上转型是将子类对象赋值给父类引用（例如 `Animal a = new Dog();`）。

| 现象 | 含义 | 示例 |
|---|---|---|
| **遗忘** | 父类里没有的方法，上转型后就用不了了。 | `fetchBone()` 是 Dog 独有的，Animal 不认识，调不了。 |
| **重写/覆盖** | 父类里有的方法，如果子类也写了一个同名同参数的，就按子类的来。 | `bark()` 两边都有，执行的是 Dog 重写的"汪汪汪"。 |

> 💡 **灵魂拷问：上转型了，狗还是汪汪汪叫，那上转型作甚？**
> **答**：上转型的意义**不是为了改变行为**，而是为了**统一管理不同的子类**。
> 没有上转型，每加一种动物就要多写一个处理方法（100种动物写100个方法）。有了上转型，只需要写一个 `void feed(Animal a)`，不管以后加猪还是加羊，这一行代码永远不用改。"方法只写一次，动物随便加"。

---

## 四、抽象类 (Abstract Class)

### 基本规则
1. 用 `abstract` 修饰符定义抽象类和抽象方法。
2. 抽象方法**只声明，不需要实现**（以分号 `;` 结尾，没有大括号）。
3. 包含抽象方法的类必须是抽象类。
4. 抽象类中可以包含普通的方法。
5. 抽象类不能直接 `new`，通常是定义引用变量指向子类对象（上转型）。

### 抽象类的继承账本

如果一个类继承了抽象类，它必须实现所有的抽象方法，除非它自己也是抽象类。

| 方法 | 爷爷 Telephone (抽象类) | 爸爸 SmartPhone (抽象类) | 孙子 iPhone (普通类) |
|---|---|---|---|
| `call()` | 抽象 | ✅ 爸爸实现了 | 继承爸爸的，不用管 |
| `message()` | 抽象 | 没实现，往下传 | ✅ **必须实现** |
| `wifi()` | 不存在 | 抽象（新加的） | ✅ **必须实现** |

> 💡 **灵魂拷问：子类是抽象类可以不用完全实现留给后代，那可不可以再写一个自己的抽象方法？如果孙子不是抽象类，是不是得把爸爸和爷爷所有的抽象方法都实现？**
> **答**：可以自己新加抽象方法。孙子不是抽象类，所以它必须把家族里所有还没实现的抽象方法**全部还清**。不管是爷爷欠的还是爸爸欠的，到普通类这一代必须全部结清。

---

## 五、接口 (Interface)

### 接口合并与实现

| | `interface extends 接口` | `class implements 接口` |
|---|---|---|
| **做了什么** | 合并多份合同，变成一份更大的合同 | 签合同，把所有条款落实成代码 |
| **需要写方法体吗** | ❌ 不需要 | ✅ 必须写 |

> 💡 **灵魂拷问：implements 不是可以继承多个接口吗，那干嘛用 extends 合并成一个，不是多此一举吗？**
> **答**：当很多类都需要同一组接口的时候，合并能省重复代码。更重要的是**可以用来当方法参数统一处理**。
> 如果没有合并，你想写一个方法要求传进来的对象**既会游泳又会跑步**，参数类型填什么？填 `Swimmable` 调不了跑，填 `Runnable` 调不了游。但是合并成 `Athlete` 之后，写成 `void 比赛(Athlete a)`，一个类型就搞定了两种能力的要求。

---

## 六、内部类与外部类

### 逻辑关系
内部类与外部类一般是 **整体与部分** 或者 **拥有者与附属品** 的关系。
> 内部类存在的前提是：**它离开外部类就没有意义。**

> 💡 **灵魂拷问：“使用外部类对象创建内部类对象，对象名为b”，那一般创造对象没有”外部类对象.“，是什么在创造呢？**
> **答**：普通类（比如 `Student b = new Student()`）是独立的，自己能活。
> 而内部类必须寄生在外部类对象上才能活。因为内部类往往需要直接访问外部类的私有属性，如果没有外部类对象，它根本不知道去哪找这个属性。`a.new Student()` 的意思就是：创建一个属于 `a` 的 Student。

### 同名变量的访问语法
如果外部类和内部类具有相同的成员变量，内部类默认访问自己的（就近原则）。如果要访问外部类的变量，语法如下：

```java
this.name              // 内部类自己的属性
外部类名.this.name     // 外部类的属性
```
