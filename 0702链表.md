# 🏆 终极复盘：链表打怪通关秘籍

> 这份复盘不是冷冰冰的代码，而是你今天自己推导出来的心法。你可以把它当成考场上的**“防断片指南”**。

## 一、 核心画面感：链表就是“小人牵手”
不要把 `->next` 当成冰冷的指针，把它当成**“右手牵着后面的人”**。
做任何链表大题（插入、删除），第一步永远是在草稿纸上画三个圈，画上箭头。脑子里有了“谁松开了手，谁牵上了谁”的画面，代码自然就流出来了。

## 二、 单链表插入的“生死铁律”
**口诀：先完善自己，再瞻前顾后。**

**动作分解：** 拿着新号码牌的 `s` 要插队到 `curr` 后面。
*   **正确顺序：**
    1. `s->next = curr->next;` （`s` 先伸出手，死死牵住后面的老哥）
    2. `curr->next = s;` （前面的 `curr` 松开原来的手，牵住新来的 `s`）
*   > [!WARNING]
    > **防坑预警**
    > 这两步**绝对不能反**！如果 `curr` 先松手去牵 `s`，原来排在后面的所有人就全走丢了（丢失指针，俗称“断链”）。

## 三、 宏观架构的觉醒：“包工头”与“打工人”
你今天完成的最惊艳的跨越，是懂得了**面向对象封装**的本质：
*   **包工头（`LinkList`）**：他不干粗活，不存数据。他只握着几条关键的缰绳：开头在哪（`front`）、结尾在哪（`rear`）、当前视察到哪了（`curr`和`pre`）。
*   **打工人（`LinkNode`）**：这才是真正在一线干活的节点，装填数据（`data`）并负责连线（`next`）。

> [!TIP]
> **实战意义**
> 考试时，只要看到函数参数传的是 `LinkList* llist`，你立刻就能反应过来，必须通过 `llist->front` 这样的方式去下达指令，绝不会犯 `llist->next` 这种小白错误。

## 四、 双向链表存在的唯一真理
**考点：为什么要有双向链表（前驱指针 `prior`）？**

**答案：为了在删除节点时，实现 $O(1)$ 的“光速拔插”。**
在单链表中，你想删除自己，却死活联系不到牵着你的“前一个人”，只能从头再找一遍（$O(n)$）。而在双向链表里，你可以直接喊一句：`p->prior->next = p->next`（把我前面的人的手，交给我后面的人），优雅且安全地完成自我删除。

---

## 💡 考前心理按摩
你今天推导出的逻辑，证明你已经具备了**“从物理图象翻译成代码”**的能力，这比背诵十个万能模板都强。
明天下午的考场上，不管链表题变出什么花样，只要你觉得脑子有点乱，**立刻停下来，在卷子上画那几个牵手的圆圈**。图一画出来，分数就已经是你的了。
