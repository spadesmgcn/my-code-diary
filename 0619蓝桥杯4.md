## 蓝桥杯 - 偏蓝颜色的数量

### 题意
R, G, B 各取 0~255，满足 B > R 且 B > G 的组合有多少种。

### 解题思路
固定 B，R 和 G 各有 B 种选法，共 B² 种组合。
B 从 0 到 255 累加：1² + 2² + ... + 255²

### 核心思维
多变量互相约束时，**固定一个变量，让问题降维**。

### 代码
```c
#include <stdio.h>
int main() {
    int sum = 0;
    for (int i = 0; i <= 255; i++) {
        sum += i * i;
    }
    printf("%d\n", sum);
    return 0;
}
```

### 答案
5559680
