## LeetCode 2090 - K Radius Subarray Averages

### 题意
以每个下标 `i` 为中心，取左右各 `k` 个元素，共 `2k+1` 个，求平均值。
不足 `k` 个的位置返回 `-1`。

### 解题思路
定长滑动窗口，窗口大小 `2k+1`，右指针跑到 `i` 时，中心是 `i-k`。

### 踩过的坑
- `out` 写成了下标 `i-2*k`，应该是值 `nums[i-2*k]`
- `malloc` 之后要初始化为 `-1`，否则无效位置是随机值
- `sum` 要用 `long long`，防止溢出（最大约 2×10¹⁰）

### 最终代码
```c
int* getAverages(int* nums, int numsSize, int k, int* returnSize) {
    long long sum = 0;
    int len = 2 * k + 1;
    int* avg = (int*)malloc(numsSize * sizeof(int));
    for (int i = 0; i < numsSize; i++) avg[i] = -1;
    for (int i = 0; i < numsSize; i++) {
        sum += nums[i];
        if (i < 2 * k) continue;
        int center = i - k;
        avg[center] = sum / len;
        sum -= nums[i - 2 * k];
    }
    *returnSize = numsSize;
    return avg;
}
```
