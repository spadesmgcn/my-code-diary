# 数据结构期末考点精编：线性表、栈与队列

> 💡 **核心心法**：不要死记代码，在脑海中建立物理图像。数据结构的本质就是“拿纸条（指针）牵手”和“格子间的游标移动”。

---

## 一、 线性表 (Linear List)

### 1.1 顺序表 (Sequential List)
*   **核心特性**：底层是连续内存（数组）。最大的优势是**随机访问**，通过下标 `data[i]` 访问任何元素的时间复杂度都是 **$O(1)$**。
*   **插入操作**：要在第 $i$ 个位置插入，必须把后面所有的元素**从后往前**依次向后挪一位，腾出空间。时间复杂度 **$O(n)$**。
*   **删除操作**：要删除第 $i$ 个元素，必须把后面所有的元素**从前往后**依次向前挪一位，覆盖掉目标。时间复杂度 **$O(n)$**。

### 1.2 链表 (Linked List)
*   **核心对比**：链表检索慢（$O(n)$），但如果已知当前节点，插入和删除极快（$O(1)$）。
*   **插入节点生死铁律**：先完善自己，再瞻前顾后。必须先 `s->next = curr->next;`，再 `curr->next = s;`，这两步顺序绝不能反，否则会断链。
*   **双向链表最大优势**：在已知当前节点指针的情况下，进行自我删除或在前面插入，时间复杂度从单链表的 $O(n)$ 降为 **$O(1)$**（空间换时间）。
*   **判空防坑**：在不带头结点的单链表中，判断链表“至少有一个节点”的条件是 `L != NULL`。切忌写 `L->next != NULL`（空表时会导致空指针异常死机）。

---

## 二、 栈 (Stack)

### 2.1 顺序栈 (SeqStack)
*   **存储特征**：使用连续数组（如 `data[0...max-1]`），没有指针和内存泄漏的烦恼。
*   **游标设计**：通常初始 `top = -1`。
*   **进栈 (Push)**：先移游标再落座。`top++; data[top] = x;`
*   **出栈 (Pop)**：先拿数据再移游标。`x = data[top]; top--;`
*   **判满**：`top + 1 == max`

### 2.2 链栈 (LinkStack) 
*   **无头结点的链栈**：`top` 指针直接指向栈顶第一个真实数据。
*   **进栈 (Push)**：相当于单链表的**头插法**。`s->next = top; top = s;`
*   **出栈 (Pop) 必背套路**：必须设替身防止内存泄漏和找不到路。
    ```c
    LNode *p = top;       // 1. 设替身抓住旧栈顶
    top = top->next;      // 2. 游标安全下移
    free(p);              // 3. 安全释放旧节点内存
    ```

---

## 三、 队列 (Queue)

### 3.1 循环队列 (Circular Queue)
选择填空题的重灾区，背熟“吃豆人”三大公式即可无脑拿分：
*   **核心逻辑**：为了区分队空和队满，必须**牺牲一个物理空间**。若数组物理总格子数为 `max`，实际最多只能存 `max - 1` 个元素。
*   **步进公式**：` (index + 1) % max ` (永远对物理总格子数求余)
*   **判满公式**：` (rear + 1) % max == front ` (rear 往前试探性跨过最后一个空格，正好踩到 front，说明队满)
*   **长度公式**：` (rear - front + max) % max ` (口诀：尾减头，加圈模圈)

### 3.2 循环链队 (Circular Linked Queue - 仅用尾指针)
*   **魔法身份映射**：首尾相连的圈，一个 `rear` 掌控全局。
    *   **队尾在哪里**：`rear`
    *   **废话头结点在哪里**：`rear->next`
    *   **真正队头数据在哪里**：`rear->next->next`
*   **出栈特大雷区**：当队列中只剩最后一根独苗（即 `first == rear`）时，出队后一定要手动复位尾指针：`rear = head;`，否则 `rear` 会变成野指针导致崩溃。
