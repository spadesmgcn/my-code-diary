# Java 复盘笔记 (06月25日)

> [!TIP]
> 核心策略：背诵骨架代码，理解业务逻辑。面向对象和异常处理是期末编程大题的必考项。

## 1. 匿名类 (Anonymous Class)
专门用于只使用一次的场景，本质上是**没有名字的内部类**。

- **三大步骤（“把正常对象切开”）**：
  1. 假装正常 `new` 对象：`Parent p = new Parent();`
  2. 塞入大括号：`Parent p = new Parent() { };`
  3. 在括号内重写方法。
- **基于父类 vs 基于接口**：
  - 基于接口：完全重写抽象方法。
  - 基于父类：不仅可以重写，还可以在内部使用 `super.方法()` 调用父类的原有逻辑。
  - **雷区**：`super` 只能写在大括号里面的方法中，**绝不能写在外部**。

## 2. 异常的族谱与两大流派
所有的报错老祖宗是 `Throwable`，死神大儿子是 `Error`（不用管，管不了），程序员天天打交道的是小儿子 `Exception`。

> [!WARNING]
> 期末极其爱考两种 Exception 的区别：
- **运行时异常 (RuntimeException)**：比如除以 0、空指针。**怪程序员没写好逻辑。** 编译器默认信任你，不强制要求处理。
- **受检/编译时异常 (Checked Exception)**：比如找不到文件。**怪外部环境。** 编译器绝不信任你，强制要求你写 `try-catch` 或者甩锅 `throws`，否则直接标红不让运行。

## 3. `throw` 与 `throws` 的本质区别
记住“**动词原形 vs 第三人称单数**”的神级理解法！

- **`throw` (动词原形)**：
  - **位置**：写在方法的大括号 `{}` 内部。
  - **作用**：主动拉响炸弹。
  - **语法**：`throw new ArithmeticException("除数不能为0");`
- **`throws` (第三人称单数)**：
  - **位置**：写在方法名和参数的括号后面。
  - **作用**：门上的警告牌，告诉调用者“本方法（第三人称）会扔出异常，出了事我甩锅，你来背”。
  - **语法**：`public void test() throws Exception`

## 4. 多重 catch 的排列顺序（你漏掉的补充点 1）
如果出现多个 `catch` 块捕捉异常：
- **铁律**：必须按照“从小到大”、“从子类到父类”的顺序排列！
- **原因**：如果把大网（`Exception`）放在第一位，它会拦截所有异常，导致排在后面的小网（如 `ArithmeticException`）变成无法到达的死代码（Unreachable code），引发编译错误。

## 5. 自定义异常的“三步傻瓜模板”
编程大题直接套用的 5 行代码模板：

```java
// 1. 类名以 Exception 结尾
// 2. 继承 Exception (受检异常) 或 RuntimeException (运行时异常)
public class MyException extends Exception { 
    // 3. 构造方法里调用 super()
    public MyException(String message) {
        super(message); 
    }
}
```

## 6. Scanner 的极度危险大坑（你漏掉的补充点 2）
关于 `next()` / `nextInt()` 与 `nextLine()` 的混用陷阱：
- **`next()` / `nextInt()`**：遇到空格或回车就停止，并且**会把回车符留在输入管道里**。机试读数字必备。
- **`nextLine()`**：一口气读到底，直到遇到并吃掉回车符。
- **💥 致命雷区**：如果在写了 `nextInt()` 之后，立刻写 `nextLine()`，这个 `nextLine()` 会瞬间吃掉刚刚残留的回车符，导致你连输入的机会都没有就直接跳过了。
  - **破解法**：在这两句之间，加一句空的 `sc.nextLine();` 把残留的回车符消耗掉。

## 7. 面向对象设计题核心（你漏掉的补充点 3）
应对 20 分的“设计类大题”（例如计算各种图形面积）：
- **开闭原则**：对扩展开放（随时能加新图形），对修改封闭（不改老代码）。
- **套路**：抽取一个 `Shape` 接口，里面放 `getArea()` 抽象方法。让不同的图形（如 `Circle`, `Triangle`）去 `implements Shape` 并重写。这就是最完美的设计。

---
