# feat: leetcode 2586 vowel strings

- char** 当二维数组用 words[i][j]
- 最后一个字符用 strlen(words[i])-1，不是 wordsSize
- 判断元音抽成独立函数，可读性更好

  ```c
  bool isYY(char w){
    return w=='a' ||w=='e' ||w=='i' ||w=='o' ||w=='u' ;
  }
  int vowelStrings(char** words, int wordsSize, int left, int right) {
  
      int count=0;
      for(int i=left;i<=right;i++){
          int len=strlen(words[i]);
          if(isYY(words[i][0]) && isYY(words[i][len-1]) ){
              count++;
          }
          
      }return count;
  }
  
*最后:今天被克劳德狠狠凶了一把*
