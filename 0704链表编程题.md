# 链表算法费曼速通手册

> 算法思路 + 源码 + 逐行解释，一体化，今晚拿下！

---

## 万能口诀（先背这个）

```
链表删结点  → 必须抓前驱
尾插法      → 接尾巴，tail跟上，顺序不变
头插法      → 插头部，next先保存，顺序自动反转
有序插入    → 内层while找位置，两行标准插入
指针操作    → 先保存 p->next，再移动指针！！！
```

---

## 一、交叠链表（交叉合并两条链表）

**题意**：`[1,2]` + `[5,6,7,8]` → `[1,5,2,6,7,8]`

**思路**：用 `tail` 追踪新链表末尾，轮流从 `p1`、`p2` 各取一个结点接上去，谁先用完就把剩余的直接接尾巴。

```c
struct Node* fun(struct Node* line1, struct Node* line2)
{
    // 新建一个空头结点，它不存数据，只是新链表的入口
    struct Node* newHead = (struct Node*)malloc(sizeof(struct Node));

    // tail始终指向新链表最后一个结点，现在链表是空的所以指向头结点自己
    struct Node* tail = newHead;

    // p1、p2分别指向两条链表的首结点（跳过头结点）
    struct Node* p1 = line1->next;
    struct Node* p2 = line2->next;

    // 只要两条链表都还有结点，就继续交叠
    while(p1 != NULL && p2 != NULL) {

        tail->next = p1;    // 把p1当前结点接到新链表末尾
        p1 = p1->next;      // ⚠️ 先让p1记住下一个！（tail马上要走到p1位置）
        tail = tail->next;  // tail走到刚接上的p1结点，它现在是新链表最后一个

        tail->next = p2;    // 把p2当前结点接到新链表末尾
        p2 = p2->next;      // ⚠️ 先让p2记住下一个！
        tail = tail->next;  // tail走到刚接上的p2结点
    }

    // 哪条链还有剩余，直接整体接上（它们本身已经是连好的链了）
    if(p1 != NULL) { tail->next = p1; }
    if(p2 != NULL) { tail->next = p2; }

    // 返回头结点（通过newHead->next访问第一个有效结点）
    return newHead;
}
```

### ⚠️ 最容易错的地方

```c
// ❌ 错：先移tail，p->next就找不到了
tail->next = p1;
tail = tail->next;
p1 = p1->next;    // 此时p1和tail指向同一个结点，p1->next已被后续操作破坏

// ✅ 对：先让p走，再移tail
tail->next = p1;
p1 = p1->next;    // 先保存
tail = tail->next; // 再移动
```

---

## 二、删除最小值结点

**题意**：找到链表中值最小的结点并删除（最小值唯一）。

**思路**：双指针同步走，一对`(p, pre)`负责遍历，另一对`(minp, minpre)`负责记录最小值及其前驱。遍历完直接用`minpre`删掉`minp`。

```c
void DeleteElem(LinkList *&L) {

    // p从首结点开始遍历，pre是p的前驱，从头结点开始
    LNode *p = L->next;
    LNode *pre = L;

    // 先假设首结点是最小值
    LNode *minp = p;
    LNode *minpre = L;    // ⚠️ minpre必须记！链表不能反向找前驱！

    // 遍历整条链表
    while (p != NULL) {
        if (p->data < minp->data) {  // 找到更小的
            minp = p;                 // 更新最小值结点
            minpre = pre;             // ⚠️ 同步更新其前驱！
        }
        pre = p;           // pre跟上来（pre始终是p的前驱）
        p = p->next;       // p前进
    }

    // 找到了，用前驱删除它（绕过minp）
    minpre->next = minp->next;
    free(minp);            // 释放内存
}
```

### 图解删除

```
删除前：
L → [3] → [1] → [4] → [2] → NULL
     ↑      ↑
   minpre  minp（最小值=1）

minpre->next = minp->next
即：[3]->next = [4]

删除后：
L → [3] → [4] → [2] → NULL
```

---

## 三、删除最大值结点

**题意**：和删除最小值完全一样，只是把 `<` 改成 `>`。

```c
void delmaxnode(LinkNode *&L) {

    // 同样的双指针套路
    LinkNode *p = L->next, *pre = L, *maxp = p, *maxpre = pre;

    while (p != NULL) {
        if (maxp->data < p->data) {  // 只改了这里！找到更大的
            maxp = p;
            maxpre = pre;            // 前驱同步更新
        }
        pre = p;
        p = p->next;
    }

    // 删除最大值结点
    maxpre->next = maxp->next;
    free(maxp);
}
```

> 💡 **这两道题是同一道题**，换个符号而已。记一个等于记两个。

---

## 四、交替拆分链表（第二条反转）

**题意**：`[1,2,3,4,5,6]` → L1:`[1,3,5]`（顺序）+ L2:`[6,4,2]`（反转）

**思路**：
- L1 用**尾插法** → 顺序不变
- L2 用**头插法** → 自动反转

```c
void splitReverse(LinkList L, LinkList *L1, LinkList *L2) {

    LNode *p = L->next;   // p从首结点开始

    // 新建两个头结点
    *L1 = (LinkList)malloc(sizeof(LNode));
    *L2 = (LinkList)malloc(sizeof(LNode));

    LNode *tail1 = *L1;   // L1需要尾指针（尾插法必备）
    (*L2)->next = NULL;    // ⚠️ 头插法必须先把头结点的next置NULL

    while (p != NULL) {

        // === 奇数位 → 尾插到L1（顺序不变）===
        tail1->next = p;   // 接到L1末尾
        p = p->next;       // ⚠️ 先保存下一步
        tail1 = tail1->next; // tail1跟上

        // === 偶数位 → 头插到L2（顺序反转）===
        if (p != NULL) {
            LNode *tmp = p->next;   // ⚠️ 先保存p的下一个
            p->next = (*L2)->next;  // p指向L2原来的第一个结点
            (*L2)->next = p;        // 头结点指向p，p成为新的第一个
            p = tmp;                // p继续往后走
        }
    }

    tail1->next = NULL;   // ⚠️ L1尾部必须收尾！
    // L2不需要收尾，头插法保证最后插入的结点的next指向NULL
}
```

### 头插法三行口诀

```
① tmp = p->next        先记住下一个
② p->next = 头结点后面  p插到最前面
③ 头结点->next = p      p成为新的第一个
```

```
头插[2]：  L2 → [2] → NULL
头插[4]：  L2 → [4] → [2] → NULL
头插[6]：  L2 → [6] → [4] → [2] → NULL  ← 自动反转！
```

---

## 五、链表插入排序

**题意**：将无序链表原地排成升序。

**思路**：
1. 把第一个结点单独留出来，当作"已排好序的链表"
2. 从第二个结点开始，每次取一个结点
3. 在有序链表里找到正确位置，插进去
4. 重复直到所有结点都归位

```c
void sort(LinkNode *&L) {

    LinkNode *p, *pre, *q;

    p = L->next->next;      // p从第2个结点开始（第1个结点留着当有序链表的初始内容）
    L->next->next = NULL;   // ⚠️ 把第1个结点的next断掉，构造只有1个结点的有序链表

    while (p != NULL) {     // 遍历原链表剩余的每一个结点

        q = p->next;        // ⚠️ 先保存p的下一个！p马上要被插入有序链表，next会被改掉

        pre = L;            // pre从头结点开始，准备在有序链表里找插入位置
        // 找到第一个"不比p小"的结点的前驱
        while (pre->next != NULL && pre->next->data < p->data)
            pre = pre->next; // pre一直往后走，直到找到正确位置

        // 标准链表插入：在pre后面插入p
        p->next = pre->next; // p指向pre原来的后继
        pre->next = p;       // pre指向p，p插进来了

        p = q;              // p继续处理下一个待插结点
    }
}
```

### 图解插入过程

```
初始有序部分：L → [3] → NULL
待处理：[1] → [2] → [5]

取1，找位置：头结点后面的3 > 1，所以插到头结点后面
有序：L → [1] → [3] → NULL

取2，找位置：1<2，3>2，插到1后面
有序：L → [1] → [2] → [3] → NULL

取5，找位置：1<5，2<5，3<5，走到NULL，插到3后面
有序：L → [1] → [2] → [3] → [5] → NULL  ✅
```

---

## 总结：五道题的核心套路

| 题目 | 关键套路 | 最容易忘的 |
|------|----------|------------|
| 交叠合并 | 双指针轮流接 | p先走，tail再跟 |
| 删最小/最大 | 双指针+记前驱 | minpre/maxpre同步更新 |
| 交替拆分反转 | 尾插+头插组合 | L2初始化next=NULL，L1末尾置NULL |
| 插入排序 | 内层while找位置+标准插入 | q=p->next先保存，两行标准插入 |

---

## 标准链表插入（背熟！所有题都用）

```c
// 在pre后面插入p
p->next = pre->next;   // 第一行：p先指向pre的后继
pre->next = p;         // 第二行：pre再指向p
// 顺序绝对不能反！
```

## 标准链表删除（背熟！所有题都用）

```c
// 删除pre后面的结点q
q = pre->next;         // 先记住要删的结点
pre->next = q->next;   // 绕过q
free(q);               // 释放
```

---

> 🔥 **最后一句话**：链表题不怕难，怕的是没框架。认出套路 → 套模板 → 改细节。加油，今晚拿下！
